language: node_js
node_js:
  - "8.11.2"
branches:
  only:
    - master
    - staging
    - develop
before_script:
  # NOTE: Configures dotenv for tests. See comment in `configureTestEnv.js`
  - cp .env.example .env
script:
  - yarn run lint
cache:
  yarn: true
  directories:
    - node_modules
